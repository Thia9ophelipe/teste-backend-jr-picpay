services:
  mysql:
    image: mysql:8.0
    ports:
      - "3307:3306"

    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: picpaydb

    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: